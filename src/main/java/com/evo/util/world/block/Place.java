package com.evo.util.world.block;

public enum Place {
    /**
     * Places using the playerController with the extra checks
     */
    VANILLA,

    /**
     * Places with only packets
     */
    PACKET
}
